Read `AGENTS.md` first — it is two lines, and it is the source-of-truth rule
every agent here is held to. Then read `CONTRIBUTING.md` before writing code:
it carries the component-system and reuse rules, the build step for
`shell/src`, and the hard rule about running tests before every push.

Nothing in this file overrides either of them.
